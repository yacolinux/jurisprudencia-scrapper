{
  "name": "mcp-jurisprudencia-corrientes",
  "version": "0.1.0",
  "private": true,
  "description": "MCP determinista para consultar la jurisprudencia pública del Poder Judicial de Corrientes.",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "start:xvfb": "xvfb-run -a --server-args='-screen 0 1280x900x24 -nolisten tcp' node src/server.mjs",
    "test": "node --test"
  }
}
